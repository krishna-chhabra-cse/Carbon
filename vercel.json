{
  "buildCommand": "cd \"apps/Carbon Frontend\" && npm install && npm run build",
  "outputDirectory": "apps/Carbon Frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
